spring:
  application:
    name: recommendation
  config:
    import:
      - optional:configserver:http://localhost:7071
server:
  port: 7002

management:
  endpoints:
    web:
      exposure:
        include:
          - busrefresh
          - health
