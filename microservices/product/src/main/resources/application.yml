spring:
  application:
    name: product
  config:
    import:
      - optional:configserver:http://localhost:7071
server:
  port: 7001

management:
  endpoints:
    web:
      exposure:
        include:
          - busrefresh
          - health
